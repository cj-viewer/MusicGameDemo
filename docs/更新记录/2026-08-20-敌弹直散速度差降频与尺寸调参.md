# 2026-08-20：敌弹直散速度差、降频与尺寸调参

- 需求：直射敌弹更快、散射保持原速；所有敌人稍微降低开火频率；敌弹视觉和碰撞体一起放大，缺失项加入 P 面板。
- 实现：保安 / 粉丝基础弹速保持 `144 px/s`，单发直射使用新增 `1.25x` 倍率到 `180 px/s`，粉丝三弧散射仍为 `144 px/s`；攻击频率调整为 `0.75 / 0.20 次/拍`。新增敌弹统一尺寸 `1.20x`，同步缩放可见 Image、主 Body 和三点碰撞盒。P 面板新增直射弹速倍率、敌弹视觉 / 碰撞尺寸，既有两类频率调节改为 `0.05` 步进；战斗参数 TXT 同步导出两项新键。
- 影响：中型保安沿用保安参数，教学角色沿用粉丝参数；节拍突进、伤害、寿命、颜色、三弧数量与 `10%` 概率不变。
- 验证：`npm run build` 通过；本地浏览器确认 P 面板默认值、按钮步进和教学敌弹显示，控制台 `0 error / 0 warning`。
- 遗留：无。
