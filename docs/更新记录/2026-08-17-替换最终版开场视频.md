# 2026-08-17 / 替换最终版开场视频

## 需求

- 使用 `C:\Users\angel\Desktop\GameJam\Final Intro.mp4` 替换游戏当前开场动画。
- 保留现有标题页、播放与跳过流程，并提供本地服务器测试地址。

## 实现

- 将新视频写入 `public/assets/video/intro.mp4`，保持既有运行时文件名、Phaser 资源键和 `import.meta.env.BASE_URL` 路径拼接方式不变。
- 新视频大小为 `29,913,417` 字节，SHA-256 为 `BCBC6C8046799CF7B9AA64CAE7C77CC1E92C6E997CAF5EF84393C54D27470F89`。
- 未保留桌面源文件名中的空格和 `Final` 后缀，符合运行时素材命名规范。

## 影响

- 玩家点击标题页“开始”后将看到新版开场视频。
- 带原声播放、浏览器媒体解锁、`Space` 跳过、播放完成后进入教学等代码逻辑不变。
- 素材作者及公开发布授权仍需项目方确认。

## 验证

- `npm run build`
- 本地开发服务器首页与 `/assets/video/intro.mp4` 均应返回 `200`，视频响应类型应为 `video/mp4`，长度应为 `29,913,417` 字节。
