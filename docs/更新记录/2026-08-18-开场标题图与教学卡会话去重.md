# 2026-08-18：开场标题图与教学卡会话去重

## 需求

- 用项目方提供的完整插画替换程序化开场标题页，并让“开始”按钮适配画面中的留白。
- 第一关侧边四张操作任务卡依次出现；每张卡只要已经出现过，本次页面会话内便不再重复出现。

## 实现

- 新增 `public/assets/images/backgrounds/intro/intro-title-background.png`，保持项目方 `1674 x 940` RGBA 原图不变。`IntroScene` 以居中 Cover 方式铺满逻辑画布，删除旧程序化背景、光圈、标题和副标题。
- “开始”按钮改为逻辑坐标 `(1060,230)`、尺寸 `220 x 60`，使用半透明莓红底与双层浅色细框，放在右上粉色天空留白中。
- 四张教学卡保留“左键 → 右键 → Esc → Shift”的顺序与原位 Alpha 动画。卡片的有效淡入开始时写入 Scene 实例级已展示集合；教学重练或 `R` 重开时从第一张未展示卡继续，四张全部展示后不再显示。完整页面刷新后新建 Scene，展示记录随之重置。

## 影响

- 点击开始、两段片头顺序、媒体解锁、`Space` 跳过、教学输入与节拍判定均未改变。
- 开场标题图由 `IntroScene.preload()` 单独加载，不进入 core 清单。当前 core 仍为 `144` 张图片 + `3` 个音频、共 `147` 个 Loader 项和 `13,069,079` 字节。
- 图片 / GIF 总数为 `217`：背景 `15`、角色 `151`、环境物件 `35`、武器 `8`、教学 UI `8`。

## 验证

- `npm.cmd run build` 通过。
- 本地 `http://127.0.0.1:5180/` 确认新开场插画按比例铺满，按钮不遮挡标题、角色或舞台；按钮点击与 `Space` 跳过仍能进入片头 / 教学。
- 依次完成左键、右键、Esc、W + Shift 后，四张卡全部消失；等待超过 `7s` 没有循环重现。代码路径复核确认重练与 `R` 重开不会清空已展示集合。
- 开场图、教学 UI 与场景资源请求成功；浏览器控制台无 error / warning。

## 遗留

- “每张只显示一次”的范围为当前浏览器页面会话；完整刷新页面会重新展示教学卡。若后续要求跨刷新永久记忆，需要另行增加本地存储与清除入口。
