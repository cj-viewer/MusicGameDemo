# 2026-08-21：精简帧动画恢复

- 需求：角色序列帧继续正常播放，不完全关闭动画，同时保留背景静态化、弹幕回收、资源释放和其他 gameplay 性能优化。
- 实现：使用压缩后仍保留的帧重新注册 Phaser 动画。玩家 Idle / Run 各 4 帧、Dash 6 帧、两套死亡各 4 帧；粉丝与保安 Idle / Run 各 4 帧；教学角色 Idle / Run 各 4 帧、Roll 6 帧。玩家轻重攻击各 5 帧，粉丝 / 教学轻重特效为 5 / 6 帧，保安为 5 / 7 帧。
- 影响：没有恢复已删除的 8 / 12 帧冗余素材；静态 WebP 背景、场景特效降载、敌弹出界回收、教学资源释放及其他性能路径保持不变。
- 验证：`npm.cmd run build` 通过；浏览器分别对比教学与 Wave 1 相隔 `260ms` 的连续画面，玩家、教学角色、保安、粉丝及攻击特效均发生帧变化，控制台 `0 error / 0 warning`。
- 遗留：后续若正式关仍卡顿，应继续采样单位 / 弹幕逐帧开销，不再以完全关闭角色动画作为默认手段。
