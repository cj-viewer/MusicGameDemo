# 2026-08-16：结束待机、分类音量、战斗参数与连段反馈

## 需求

- 检查 Git 历史并撤销今天已有修改，再从稳定基线继续开发。
- 游戏结束时冻结所有单位并播放 Idle，清除场上所有飞行子弹。
- 按音效类别分别控制音量，并在设置中开放玩家 / 敌人武器弹速与攻击速度。
- 判断并关闭玩家自动攻击。
- 连续正确踩拍时让中央扩张框动态积亮，拍间回落，完成四拍 Combo 时增加边缘溅射奖励。

## 实现

- `dev-1.0` 在 2026-08-16 没有新提交，HEAD 为 `0109725`（2026-08-15）。未重写历史；已把当日 9 个已跟踪工作树修改恢复到 HEAD，并移除 3 个当日未跟踪记录文件，再从干净基线实现本次功能。
- 战败入口停止玩家与所有敌人的速度、攻击动作和 Tween，固定原位进入 Idle / 静态待机；敌我弹丸、警棍弧段及命中体立即销毁。
- Esc 设置新增主音量、BGM、节拍喊声、攻击与错误、受伤与敌亡、Combo、Fever、拾取八个独立音量滑杆。
- Esc 设置新增荧光棒弹速、荧光棒 / 警棍攻击动作速度、警棍弧段飞行速度、保安 / 粉丝弹速与攻击间隔。玩家动作速度不会改变拍前 `0.2s` / 拍后 `0.1s` 节拍判定窗口。
- 删除拾取武器后的自动连段演示；拾取只切换武器并显示提示，下一次攻击必须由玩家输入。
- 正确踩拍逐次提高场地节奏亮度，拍间持续衰减，错拍 / 漏拍降亮；连续四次正确输入触发四边 ADD 光屑和闪边。后续强化把光屑由 44 个提高到 84 个并改成向内 / 向外双向喷射，额外叠加双层边框冲击波、四边宽光带、四角星爆和轻微镜头震动。
- 拉取远端 `bf5ca43` 后完成冲突整合：保留远端 `132 BPM`、四首 BGM 等比变速、`2.4` 拍场地提示曲线和 `360ms` Dash / `10ms` 拖影采样，同时恢复本节全部本地功能。

## 影响

- 影响结束态、玩家和敌人动画、弹幕生命周期、武器切换、ComboMeter 能量来源、音效混音、Esc 设置布局和节奏视觉反馈。
- 武器 pattern、伤害、波次数量和 FPV 只读规则不变；BGM、输入窗口、场地提示曲线和 Dash 参数采用远端最新规则。
- P 面板的敌弹速度兼容入口仍保留，并会同时设置保安与粉丝弹速；Esc 可进一步分别调整。

## 验证

- 合并后 `npm run build`：TypeScript 类型检查和 Vite 生产构建通过。
- `git diff --cached --check`：通过（仅有 Git 的 CRLF 转换提示）。
- 本地开发服务器 `http://127.0.0.1:5173/` 与 `bgm0.mp3` 至 `bgm3.mp3` 请求均返回 `200`。
- Chrome 无头启动检查以退出码 `0` 完成，但未输出可用的页面控制台报告；仍需人工确认分类试听、各设置控件即时生效、结束态全部单位 Idle / 全弹清空，以及连续踩拍亮度起伏与边缘溅射的主观动感。

## 遗留

- 没有新增运行时素材。
- 本次不部署、不创建提交；浏览器控制台结果以交付后的人工预览复核为准。
