# 2026-08-20：Boss 阴影收小与音符子弹尺寸调参

## 需求

- 在当前正确落点基础上继续缩小 Boss 阴影。
- 放大绿色音符阵列子弹，并加入 P Menu。

## 实现

- Boss 阴影相对角色缩放由 `70%` 调整为 `55%`。
- 音符子弹默认基础尺寸由 `78%` 调整为 `100%`。
- P Menu Boss 页新增“音符子弹大小”，范围 `50%–200%`、步进 `10%`。
- 参数加入本机持久化与 TXT `[boss]` 导出；视觉和碰撞继续统一缩放。
- Boss 页行距收紧以容纳十二项参数。

## 验证

- 按快速迭代要求未执行构建或试玩。
