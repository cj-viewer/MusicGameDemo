# 2026-08-17：粉紫音乐广场原色、阴影与安检口重抠

## 需求

- 修复上一轮透明物件没有带出原始投影、局内饱和度高于参考图，以及安检口开口内部设备和场景内容被清空的问题。
- 继续保留 35 张可独立定位的透明物件和一张不带物件的干净底图。

## 实现

- 以项目方提供的 `1920 x 1080` sRGB 完整原图作为唯一颜色源；OpenAI 内置图像编辑只生成灰度分离蒙版，最终 35 张 PNG 的可见 RGB 均逐像素复制自原图，不使用生成式重绘颜色。
- 透明蒙版把接触影与投影分配给对应物件；入口大门层强制保留开口内的左侧设备 / 人影、中央淡色场景、右侧设备 / 标牌、前排栏杆和中央雕像。
- 新增 `scripts/rebuild_pink_stage_layers.py`，统一生成 35 张 RGBA PNG、`1920 x 1080` 干净底图、运行时衔接底图和 `pinkStageProps.generated.ts` 坐标清单；底部 `object-37 / 38 / 39` 分别覆盖左右灌木与中央低墙。
- 参考原图、干净补画源、灰度蒙版及最终重组预览存放在 `docs/图片和附件/`，不参与运行时构建。
- 同步修正 README 中“进入页面后有开场标题”与“加载后直接教学”的旧表述冲突，明确为片头结束或跳过后直接进入教学。

## 影响

- 正式关继续使用 35 个独立 `Image`、负深度且无碰撞；教学关、角色、节拍、BGM、HUD、敌人和战斗参数未修改。
- `PinkStageEnvironment.ts` 改为加载 `pink-stage-runtime-base.png`，定位读取生成的 `1920 x 1080` 清单，不再按旧 `1918 x 1081` 尺寸二次缩放。

## 验证

- 35 张物件均有非零 Alpha；除 JPEG 解码本身外，可见 RGB 对参考图逐像素误差为 `0`。离线重组全画面平均绝对色差为 `0.68 / 255`。
- `npm.cmd run build` 通过。浏览器在 `http://127.0.0.1:5175/` 实机进入正式关后，确认安检口内三处场景内容、路牌 / 车辆 / 商店 / 大门投影和底部三块均显示，整体色调与参考原图一致。

## 遗留

- 环境层仍不生成碰撞；如果后续需要单独移动任一场景物件，应同时复核其投影是否仍符合新位置的光照关系。
