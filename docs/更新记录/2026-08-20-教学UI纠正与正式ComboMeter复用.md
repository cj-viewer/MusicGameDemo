# 2026-08-20：教学 UI 纠正与正式 ComboMeter 复用

## 需求

- 教学关直接复用正式第一关的 ComboMeter。
- 对话框改为白底黑字的像素画风，不使用云朵造型。
- 三句对话后同时显示轻攻击与重攻击 UI，并保持显示。
- 去掉“连续完成 3 小节”的进度 UI。

## 实现

- 删除教学专用云朵量表，教学练习阶段只打开正式 ComboMeter / 武器状态组，其余正式 HUD 继续隐藏。
- 将玩家头顶对话框改为黑白硬边像素框，保留跟随位置与原三句推进逻辑。
- 轻、重攻击卡同步淡入且完成后不消失；两项完成后仍继续原设置与闪现教学。
- 不再创建三小节进度面板；连续三套连招完成判定和延迟校准仍在后台运行。

## 影响

- 只调整教学 UI 的视觉、显示顺序与 ComboMeter 复用关系。
- 闪现能量门槛、攻击判定、教学完成条件和正式关玩法不变。

## 验证

- `npm run build` 通过。
- 按快速迭代要求未进入游戏试玩。

## 遗留

- 无。
