# 2026-08-18：合并上游 2K 并保留本地交互与双关框线清理

## 需求

- 以 upstream/dev-1.0 的最新 2K 画布、正式保安、敌人参数、编队波次、警棍与性能修正为合并基线。
- 完整保留本地已经确认的开场标题图、第一关 PSD 教学 UI、鼠标左右键攻击、四张操作卡页面会话去重、浅色磨砂设置、石子前景、第二关无黑底四拍图案与轻透白线 HUD。
- 用户已明确要求两关都不要常驻或持续扩散的全场框，因此不能让上游同日恢复的 arenaBeatCues 成为合并后的当前表现。

## 实现

- 运行画布统一为 2560 × 1440；项目方 1920 × 1080 PSD 布局在 MainScene 中使用 PSD_LAYOUT_SCALE=VIEW_WIDTH/1920，即统一乘 4/3。开始按钮按 ui(1060,230) 映射到实际中心 (2120,460)、尺寸 440 × 120。
- 当前标题继续加载 backgrounds/intro/intro-title-background.png，并保留 Cover 安全出血和菜单视差；上游 backgrounds/title/main-menu.png 仅保留为未加载历史候选。
- 恢复鼠标左键轻攻击、右键重攻击，并与 Quote / Enter 汇入相同战斗和教学推进路径。四张操作卡继续按左键轻攻、右键重攻、Esc 设置、Shift 冲刺依次出现；开始淡入即记为本页面会话已展示，重练与 R 重开均不重复。
- 第一关继续按“进度面板 → 静态状态条 → 石子 → 操作卡”绘制，石子位于底栏白线上方；设置面板保留浅色磨砂半透明，而不是深色遮罩。
- 第二关四拍区域只绘制四个当前武器图案与短时高亮，不创建黑色背板。实时 HP 和 ComboMeter / 武器状态使用轻透雾面、细白线与断角程序框，数值、逻辑和既有屏幕锚点不变；第一关 PSD 静态 HUD 不变。
- 合入正式保安 Idle / Run、警棍比例与握把校准，合入 Wave 2 / 6 / 12 / 18 / 24 及三人 / 配对编队、BGM 0.25、粉丝 10% 三弧、警棍移动攻击和扫击性能修正。
- 移除本次上游合并重新带入的 arenaBeatCues 常量、字段、创建、逐帧更新与清理链。教学和正式关均不再生成未来拍全场扩散框；不可见 ARENA 物理边界、正确输入短时判定框、Fever 临时边框、重拍边缘粒子 / 光带和四拍短效继续保留。
- core 当前为 161 张图片、两条节拍音效与教学 BGM，共 164 个 Loader 项、13,357,393 字节。图片资源文件总数为 218，其中角色类 151 包含预览 GIF；未加载标题候选不计入 core。

## 影响

- 玩家看到的运行画面与输入以本记录和“当前实机规则”为准；同日早先“移除鼠标输入”“恢复未来拍预告”“启用 main-menu 标题”等记录只说明各自提交当时的状态。
- 1920 × 1080 仍可作为 PSD 和环境源稿坐标，但不再作为当前画布或运行坐标写入验收口径。
- 合并不改变攻击窗口、伤害公式、不可见物理边界、HP / Combo 实时数据或第一关静态 HUD 的用途。

## 验证

- 四份当前入口文档已清除冲突标记，并统一为 2K、鼠标攻击、无 arenaBeatCues、第二关无黑底四拍区和轻透 HUD 的最终口径。
- 更新索引已逐一覆盖目录内全部 dated 记录，无漏链或重复链接。
- 最终合并代码执行 `npm.cmd run build` 通过；Vite 完成 28 个模块构建，未出现 TypeScript 或打包错误。
- `http://127.0.0.1:5180/` 返回 HTTP `200`。浏览器依次检查开场、第一关、浅色磨砂设置、四张任务卡推进、任务卡完成后等待与 `R` 重开不再出现，以及第二关倒计时 / Wave 1：2K 布局、石子层级、鼠标左右攻击、第二关无黑底四拍图案、轻透 HUD 与双关无循环全场框均符合当前规则。
- 浏览器控制台只有 Vite 连接和 Phaser 启动日志，`0 error / 0 warning`；本轮实机未出现资源 404。

## 遗留

- 后续如调整 2K 画布、镜头 zoom、PSD 源稿或设置面板尺寸，仍需重新检查两关屏幕锚点、磨砂取样范围与不可见物理边界。
- 保留所有同日历史记录，不回写其原始内容；以后发生取舍变化时继续新增更晚记录，而不是改写旧记录。
