# 2026-08-14：恢复运行时角色素材与空格跳过片头

## 需求

- 修复拉取最新 `dev-1.0` 后玩家、保安和粉丝素材无法按既有路径加载，导致可玩内容不可见的问题。
- 标题页和开场视频期间支持按空格跳过并直接进入教学。

## 实现

- 将 `characters_backup` 中仍被 `MainScene` 使用的玩家、保安和粉丝素材恢复到 `public/assets/images/characters/` 规范路径。
- `IntroScene` 注册 `keydown-SPACE`：无论尚在标题页还是正在播放视频，均停止片头并切换到 `MainScene`。
- 标题页和视频右下角显示低干扰的“空格跳过”提示。
- 修正 README、简化玩法策划案和实机验收中仍描述“片头后再次点击”的过期流程。

## 影响

- 角色纹理键、动画注册、显示尺寸和碰撞体不变，只恢复其运行时文件位置。
- 正常点击开始仍播放完整带原声片头；视频播放完成后仍直接进入教学。
- 空格仅在 `IntroScene` 中作为跳过键，不改变教学和正式战斗输入。

## 验证

- `npm run build` 通过 TypeScript 检查和 Vite 生产构建。
- 本地标题页按空格可直接进入教学；播放视频后按空格也可直接进入教学。
- 玩家、保安和粉丝素材返回正确 PNG，页面控制台无资源加载错误。

## 遗留

- 本次只完成本地修复，尚未提交或上传远端。
