# 2026-08-14 / BGM 内部缩放、鼠标攻击与场内操作教学

- BGM 滑杆对外默认仍显示 `100%`，内部基础混音从 `0.3` 降为 `0.1`，相同显示值下实际 BGM 响度为旧版三分之一。
- 恢复鼠标左 / 右键作为轻 / 重攻击；鼠标移动仍不控制瞄准，键盘 `Quote / Enter` 和自动瞄准规则不变。
- 教学场地左下新增低透明度键位操作卡，最终收敛为 `390 x 238px` 的精简双栏布局：左栏为 WASD / 移动和左右 Shift / 冲刺，右栏为双引号键或左键 / 轻攻击、Enter 或右键 / 重攻击、Esc / 设置；删除自动锁定说明与黄色四拍提示。
- 操作卡位于场地底图上方、角色与攻击特效下方，只在教学阶段显示；完成教学进入正式游戏后销毁。
- HUD 同步移除右上角常驻设置小字，把武器名移至 ComboMeter 右侧并放大为 `22px` 粗体；玩家可见名称统一简化为“荧光棒 / 警棍”。
- 游戏加载后直接显示教学，不再创建全屏字幕与“点击开始”；教学顶部同步删除重复的轻重按键说明并缩短背板。
- 验证：`npm.cmd run build` 已通过；本地浏览器无点击刷新后直接显示教学，确认全屏字幕和顶部重复按键说明已移除、精简操作卡两栏对齐，武器名与 FPV 互不遮挡；实测鼠标轻重攻击无运行时报错，控制台 `0 error / 0 warning`。
