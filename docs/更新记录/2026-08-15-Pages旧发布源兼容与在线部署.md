# 2026-08-15：兼容旧 Pages 发布源并恢复在线部署

## 需求

- 检查远端更新，上传当前 `dev-1.0` 改动，并构建到现有在线试玩链接。

## 实现

- 推送前确认本地与 `origin/dev-1.0` 同基点，没有远端新增提交或合并冲突。
- 首次手动部署确认 Actions 构建成功、发布失败；Pages API 显示仓库仍使用旧的 `gh-pages` 分支发布源。
- 将部署工作流改为构建 `dist/` 后通过临时 worktree 更新 `gh-pages`，不再调用与当前仓库设置不兼容的 `actions/deploy-pages`。
- 保留 `main` 推送自动部署和 `dev-1.0` 手动部署两种入口。

## 影响

- 在线地址仍为 `https://cj-viewer.github.io/MusicGameDemo/`。
- 开发分支不跟踪 `dist/`；生成产物只进入 `gh-pages` 分支。

## 验证

- 本地 `git diff --check` 和 `npm.cmd run build` 通过。
- 远端 Actions、`gh-pages` 提交与在线资源状态在部署完成后复核。

## 遗留

- 正式角色、武器、橙色粉丝和特效素材的作者与再分发许可仍待补齐；公开部署前述授权风险仍存在。
