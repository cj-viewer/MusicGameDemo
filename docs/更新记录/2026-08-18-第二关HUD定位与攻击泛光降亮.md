# 2026-08-18：第二关 HUD 定位与攻击泛光降亮

## 需求

- 把正式关底部 ComboMeter / 武器细条按最新红框目标整体移动，并继续保持细小。
- 按后续反馈把顶部四拍图案下移到反馈框线下方，待机底色改为白色，只有正确输入才恢复轻 / 重拍颜色；删除倒计时“准备…”文字。
- 统一降低敌我全部攻击自发光亮度，只保留轻微、连续、柔和的泛光感。

## 实现

### 正式 HUD

- 四拍图案保持无黑色背板和 `58%` 显示比例，最终中心为 2K 约 `(1200,130)`，放在顶部反馈框线下方，避免框线穿过图案。待机图案统一白色；正确轻拍短闪青色、正确重拍短闪黄色，`220ms` 后复白，错误输入不变色。
- 倒计时阶段把 Wave 字段设为空；开波后的 `Wave N` 移到四拍上方约 `(1200,38)`，避免与图案重叠。
- ComboMeter / 武器组设计框最终为 `(503,662,184,50)`，整组仍为 `72%`；最终 2K 外框约 `(1058,1338,265,72)`、中心 `(1190,1374)`，与用户最新红框中心一致，并沿用本轮已经收窄后的尺寸。
- Combo 圆环半径由设计值 `22` 收至 `18`；头顶 HP、实时 Combo / Fever、武器名与节拍反馈逻辑保持。

### 攻击泛光

| 路径 | 调整前 | 调整后 |
|---|---:|---:|
| 玩家 Light / Hard 攻击贴图 Alpha | `0.72 / 0.92` | `0.46 / 0.59` |
| 玩家攻击贴图 Glow / Inner / Bloom | `1.05 / 0.08 / 0.44` | `0.55 / 0.04 / 0.22` |
| 公共攻击贴图 Blur strength | `0.82` | `0.52` |
| 保安攻击贴图 Alpha / Glow / Bloom | `1 / 0.90 / 0.36` | `0.62 / 0.48 / 0.18` |
| 粉丝攻击贴图 Alpha / Glow / Bloom | `1 / 1.05 / 0.40` | `0.62 / 0.55 / 0.20` |
| 玩家直射亮芯 / 外晕 | `0.94–0.96 / 0.74` | `0.52–0.58 / 0.34` |
| 敌弹点芯混白 / 合成 / Alpha | `0.66 / ADD / 1` | `0.18 / NORMAL / 0.52` |
| 敌弹径向外晕 | `ADD / 1` | `ADD / 0.08` |
| 警棍扫击外层 / 亮芯 | `0.22 / 1` | `0.12 / 0.62` |
| 子弹拖尾 Alpha 范围 | `0.08–0.24` | `0.05–0.16` |
| 激光外层 / 中层 / 白芯 | `0.16 / 0.72 / 1` | `0.10 / 0.42 / 0.62` |
| 月牙外层 / 中层 / 白芯 | `0.20 / 0.92 / 0.95` | `0.10 / 0.52 / 0.62` |

- 冲刺 / Fever 攻击音波、Fever 清屏条与 FPV 弹点同步降亮；FPV 敌弹 Billboard Alpha 最终为 `0.38`。
- 除敌弹实体点芯改为低混白 NORMAL 合成外，其余 Glow 距离、Blur 半径、Threshold、色值、ADD 合成、纹理衰减、尺寸、动画、弹速、寿命、伤害和命中盒全部保持。

## 影响

- 第一关 PSD UI、正式关头顶 HP、场地、镜头、波次和输入不变；顶部图案尺寸保持，底部沿用本轮已收窄的外框与 `18px` 圆环，最新反馈只继续校准位置、待机色和正确命中短闪，不改玩法逻辑。
- 玩家仍为线性自发光，敌弹仍为点状自发光；只降低过曝，不取消 Bloom，也不改变攻击辨识语言。
- 没有新增或删除运行时素材，Loader 与资源数量不变。

## 验证

- `npm.cmd run build` 与 `git diff --check` 已通过。
- `npm.cmd run build` 与 `git diff --check` 通过。在 `http://127.0.0.1:5180/` 实机确认：浅粉地面上的保安青点弹与粉丝橙红点弹不再夹成纯白，只剩薄层外晕；四拍待机全白，正确轻拍实测短闪青色并在 `220ms` 后复白，重拍复用同一路径显示黄色；底部细条落在红框目标中心；顶部图案中心最终约为 2K `y=130`，图案上缘约为 `y=114`，完整落在 `y=96` 的反馈框线下方。浏览器控制台 `0 error / 0 warning`。

## 遗留

- 本轮不新增用户可调“发光强度”参数；若后续仍需主观微调，只调整当前强度常量，不改视觉尺寸或玩法参数。
