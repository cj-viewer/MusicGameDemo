# 2026-08-21 荧光棒弹速与玩家弹速上限翻倍

- 荧光棒默认弹速由当前 `520 px/s` 翻倍为 `1040 px/s`。
- 玩家弹速调参上限由 `800 px/s` 翻倍为 `1600 px/s`。
- 旧版调参界面与当前 P Menu 使用同一上限，实际 Arcade Physics 发射速度继续直接读取 `glowstickBulletSpeed`。
- 验证：`npm run build` 通过 TypeScript 与 Vite production build。
