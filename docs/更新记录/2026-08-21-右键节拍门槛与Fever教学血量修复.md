# 2026-08-21 右键节拍门槛与 Fever / 教学血量修复

- 鼠标右键、Enter 与对应手柄重击只有在当前 Pattern 需要重击且命中节拍窗口时才产生攻击；错拍或错误步骤只显示节拍错误。
- 警棍长按只有正确起手才创建后续发波计时器；漏掉松开拍后立即结束长按并停止后续攻击。
- Fever 触发时立刻把 ComboMeter 进度与 HUD 圆环清为 `0%`，Fever 持续时间本身保持原拍数逻辑。
- 教学敌弹命中保留受击短闪与冲击，但伤害固定为 `0`；进入第一关倒计时恢复满血。
- 验证：`npm run build` 通过 TypeScript 与 Vite production build，`git diff --check` 通过；本地页面 HTTP 200，刷新启动后控制台 `0 error / 0 warning`。
