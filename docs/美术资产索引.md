# 美术资产索引

> 基线：`dev-1.0` 的 `90b3adc`（“美术资产目录清理”）仅将正式 NPC 的动作目录插入了一层 `animation/`。本索引选择性回滚该目录分类：正式 NPC 的动作目录直接位于各自角色根目录；不恢复已删除的占位文件，也不移动玩家的既有正式动画目录。

## 顶层分类

| 运行时根目录 | 文件数 | 内容 |
|---|---:|---|
| `images/backgrounds/` | 1 | 场地背景 |
| `images/characters/` | 151 | 正式角色、NPC、旧素材和静态保安 |
| `images/weapons/` | 8 | 玩家、粉丝、保安的荧光棒与警棍 |
| **合计** | **160** | 当前全部图片 / GIF 美术资源 |

## 角色分类

| 目录 | PNG / GIF | 状态与用途 |
|---|---:|---|
| `characters/player/animation/{idle,run,dash,attack_light,attack_hard,death01,death02}/` | 54 / 7 | 正式玩家；`playerAnimation.ts` 运行时加载 PNG，预览 GIF 不加载 |
| `characters/npc/npc_fan01/{idle,run,attack_light,attack_hard}/` | 27 / 4 | 正式橙色粉丝；`fanAnimation.ts` 加载 Run 与攻击特效，预览 GIF 不加载 |
| `characters/npc/npc_guard01/{idle,run,attack_light,attack_hard}/` | 30 / 4 | 正式安保动作与攻击特效；`guardAnimation.ts` 加载 Light 特效，预览 GIF 不加载 |
| `characters/player/{idle,run,down}/` | 7 / 0 | 旧玩家来源，当前不在运行时状态机中 |
| `characters/fan/{run,roll,attack}/` 与 `characters/fan.png` | 17 / 0 | 旧粉丝来源，保留追溯，`FanEnemy` 不加载 |
| `characters/guard.png` | 1 / 0 | 当前 `SmallGuard` / `MidGuard` 的本体贴图 |

## 其他分类与引用

| 目录 | 文件数 | 当前引用 |
|---|---:|---|
| `images/backgrounds/arena.png` | 1 | `MainScene.ts` 场地底图 |
| `images/weapons/light_stick/player/` | 1 | 玩家荧光棒 |
| `images/weapons/baton/player/` | 1 | 玩家警棍 |
| `images/weapons/light_stick/npc_fan01/` | 1 | 粉丝荧光棒 |
| `images/weapons/{light_stick,baton}/npc_guard01/` | 2 | 已分类保安武器，暂未接入静态保安本体 |
| `images/weapons/{light_stick,baton}/` 根目录 | 2 | 旧 / 通用武器来源，暂未替代正式贴图 |

## 目录与代码索引

| 资源类别 | 权威路径 | 加载代码 |
|---|---|---|
| 正式玩家动作 | `characters/player/animation/<action>/` | `src/game/playerAnimation.ts` |
| 正式粉丝动作 / 特效 | `characters/npc/npc_fan01/<action>/` | `src/game/fanAnimation.ts` |
| 正式安保攻击特效 | `characters/npc/npc_guard01/<action>/` | `src/game/guardAnimation.ts` |
| 静态安保 | `characters/guard.png` | `src/scenes/MainScene.ts` |
| 武器 | `weapons/<weapon>/<owner>/` | `src/scenes/MainScene.ts`、`src/game/Player.ts`、`src/game/enemies.ts` |

## 维护规则

- 角色动作目录名称直接使用 `idle`、`run`、`dash`、`attack_light`、`attack_hard`、`death01`、`death02`；预览 GIF 与 PNG 帧一起放在所属动作的 `preview/`，但预览文件不登记为运行时贴图。
- 玩家维持现有 `player/animation/` 层级，避免无关重命名 61 张正式帧。新 NPC 不再新增 `animation/` 中间层，直接遵循 `npc/<角色>/<动作>/`。
- 任何资源迁移必须在同一提交同步更新本索引、`资源与素材规范.md`、全部加载路径与本地构建验证；移动后必须确认旧路径没有运行时引用。
