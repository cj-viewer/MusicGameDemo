# 资源与素材规范

## 目录边界

| 内容 | 目录 | 是否参与构建 |
|---|---|---|
| 角色、武器、特效、背景、环境立牌、UI | `public/assets/images/` | 是 |
| BGM、音效 | `public/assets/audio/` | 是 |
| 字体、谱面、关卡数据 | `public/assets/fonts/`、`public/assets/data/` | 是 |
| 策划参考图、原图、附件 | `docs/图片和附件/` | 否 |

运行时代码只可从 `public/assets/` 加载资源。Phaser 路径必须通过 `import.meta.env.BASE_URL` 拼接，不能写死 `/assets/`，以兼容 GitHub Pages 的仓库子路径。

网页右上角的“美术替换”面板属于临时验收工具：用户选择的 PNG、JPG、WebP 或 GIF 只注册为当前页面内的运行时纹理，不上传、不写入 `public/assets/`，也不得被当作已正式入库素材。背景按画布等比覆盖；玩家、保安和粉丝只替换显示纹理，不改变碰撞和玩法。需要正式提交的素材仍必须按本规范放入对应目录、登记授权并完成加载与验收。

## 角色序列帧

角色素材默认只绘制**朝右**版本；向左运动由 Phaser 的 `setFlipX(true)` 完成。

```text
public/assets/images/characters/<角色>/<动作>/<动作>-<两位序号>.png
```

示例：

```text
public/assets/images/characters/fan/run/run-02.png
public/assets/images/characters/fan/roll/roll-01.png
public/assets/images/characters/fan/attack/attack-01.png
```

- 只把实际播放的帧提交进运行时目录；站定测试帧、重复帧和废弃导出帧应在导入时清掉。
- **先校准，再接入引擎。** 导入前必须逐帧检查透明内容边界、脚底基线和像素高度；不得根据完整画布大小直接设置统一显示尺寸。
- 以一个动作作为视觉基准（本项目粉丝以跑步为基准），同一动作的所有帧必须裁掉无效透明边缘，并归一到同一内容高度；宽度按原比例变化。
- 不同动作可以使用不同内容高度。应相对基准动作人工查看后决定，例如粉丝当前为：跑步 `445px`、翻滚 `415px`、近战 `500px` 内容高度；以旧静态粉丝约 `73px` 的实际可见高度为跑步缩放基准，因此翻滚仅轻微收缩、近战适度放大，避免动作切换突变。
- 玩家（女高中生）当前为：待机 / 跑步内容高度统一 `55px`（脚底基线在帧底边），倒地单帧 `72 x 15px`；以旧静态玩家约 `63.6px` 可见高度为缩放基准（约 `1.156x`）。低分辨率像素素材放大显示时，为对应纹理单独设置 NEAREST 过滤，不修改全局渲染配置。
- 每个动作完成裁切和缩放后，至少目视检查一张基准帧和一张幅度最大的帧，确认脚底不跳、攻击不显小、透明边缘不影响比例。
- 新动作必须同时更新 `preload`、Phaser 动画注册、敌人/角色状态切换和更新记录。
- 保持透明背景，单帧建议不超过 `1MB`。

## 素材授权

| 素材 | 作者 | 授权 |
|---|---|---|
| 玩家女高中生序列帧（`images/characters/player/`，源为 Anime Schoolgirl Sprite Sheet BASIC） | Dlou Saiyan | 仅限个人非商用项目；需署名；禁止商用、转售或再分发 |
| 绘本环境立牌与天空剪纸（`images/environment/storybook/`：柱子、设备箱、花草、云层、飞鸟、流星等） | OpenAI 图像生成，由项目方参考自有图片定向生成 | 当前项目内使用；正式发布前由项目方完成最终权属复核 |
| 露天音乐广场候选拆分素材（`docs/图片和附件/素材拆分/露天音乐广场/`） | OpenAI 图像生成，由项目方提供场景图与既有素材表定向生成 | 当前仅作项目美术选型；接入运行时或正式发布前由项目方复核参考图与生成结果权属 |

- 引入新第三方素材时必须在本表登记作者与授权条款；无明确授权的素材不得提交。
- 本仓库通过 GitHub Pages 公开部署，等同对外发布；上表中任何"非商用/禁止再分发"素材在公开部署前需重新确认授权风险。

## 通用命名与验收

- 新文件使用小写英文、数字与连字符；不得使用“final”“new”“v2”或模糊的临时后缀。
- 角色/敌人放 `images/characters/`，武器放 `images/weapons/`，视觉特效放 `images/effects/`，背景放 `images/backgrounds/`，独立环境立牌放 `images/environment/`，UI 放 `images/ui/`。
- 音效放 `audio/sfx/`，BGM 放 `audio/music/`。音效键与文件名的映射须在加载代码中保持可追溯。
- 导入后执行 `npm run build`，本地预览确认资源返回 200、控制台无资源 404、实际播放帧与逻辑状态一致。
- `dist/`、浏览器测试截图、下载残留和无引用的临时导出图不得提交。
